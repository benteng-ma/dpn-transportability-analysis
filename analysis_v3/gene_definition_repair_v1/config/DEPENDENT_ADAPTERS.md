# Like-for-like historical dependencies

This implementation record follows the canonical lock and the first common-method results. It cannot be called pre-result registration. Frozen repair rules and members will not change. Historical one-sided mean-difference permutation tests and their original correction families must be separately rerun, because the new two-sided studentized test is not interchangeable with the manuscript's historical tests. Retain both, without selecting the more favorable method.

Recompute the original hDRG five-parent and three-component families, JCI ten case-state/four axon-severity and TPM sensitivity, expression-matched gene tests, three-component rat DRG/ocular contextual projection, and five source protein contrasts. Retain historical row counterparts and label source-statistic uncertainty. For protein aliases use canonical per-gene median deposited statistics rather than target-P priority; this is a descriptive statistic, not a reconstructed gene-level Wald test. Keep old source thresholds and no significance-based selection.

Recompute Batch01 pathology/ordinal and paired-nerve tasks from fixed subject keys, both the original unambiguous and exact-symbol sensitivity variants where available. Recompute Batch03 program-only reference localization, inferential reference families and fixed-marker proxy adjustment/correlation. Do not reconstruct modules, reselect markers, rerun module-only/candidate-expression measurements, or reinterpret two DPN donors as validation. Program membership exclusions in any older network/TF sensitivity are affected dependencies until individually assessed, not silently certified as unchanged.

Each adapter saves complete outputs and previous rows. Reuse numerical functions only: never invoke old main functions or their file writers against original folders. Missing prerequisites remain pending/blocked, not 'unaffected'. Gene-level background analyses using deposited statistics remain exploratory and are separate from the expression-only donor-score sensitivity.
