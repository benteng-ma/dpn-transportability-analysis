# Receipt and scope

Actual receipt and branch creation time: 2026-09-12T01:22:43Z.

The 7,566-byte configuration template was supplied as the current attachment `pasted-text.txt` and copied byte-for-byte to `RESTORED_EXECUTION_CONFIG_TEMPLATE.yaml`. Its SHA256 is `04443f0705e14f41d7f198fed16679d3966dbd24ce41d2060b55920721986dc9`. The file remains a template with null fields and is not backdated as a previous lock.

The current attachment directory does not contain `REVIEW_CN.md`, `AUDIT_METRICS.json`, an `evidence` directory, `SOURCE_VERIFICATION_NOTES.md` or the stated original handoff ZIP. The detailed Chinese audit and source-verification notes are visible in the user message and are treated as review assertions to verify independently. No byte-identity claim is made for files that were not actually attached or located.

The historical v4 directory and the corrected v3.1 baseline remain read-only. This branch performs only the requested targeted repairs and continuation work. It does not change program membership or direction, rebuild WGCNA, search additional cohorts, send correspondence, submit forms, access controlled data, publish code or update a DOI.

