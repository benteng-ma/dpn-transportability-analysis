请在现有DPN项目中找到本次完整文件夹 DPN_v4_Closeout_CodeKit。
读取 CODEX_COMPLETE_TASK.md 全文至 END_DPN_V4_CLOSEOUT_TASK，
然后读取 CLOSEOUT_CONFIG_TEMPLATE.json、SCHEMAS.md、REPORTED_BASELINE.md。
这不是只输出计划的任务，请实际适配、执行和交付。

首先核对本包MANIFEST：
python -B code/seal_bundle.py verify .
再执行合成测试（仅在代码包目录，不写入科学结果）：
python -B -m pytest -q -p no:cacheprovider tests
这些命令仅核验代码包，不代表生物学任务已完成。

实际项目必须是phase0_6_human_dpn_stage_projection。
以当前targeted_repair_2026-09-12和v3.1最终strict替换清单为依据，
新建独立closeout目录；旧数据、成员、方向和历史版本只读。
请自己依据真实文件表头和代码建立输入适配器、填入实际配置、
记录路径/hash/时间；不要让作者猜列名，也不要静默取首末列。

按完整任务执行：
C01 解释M04原始feature与唯一GeneID计数，核查注释覆盖。
仅表格口径不对就只更正报告，真有矩阵错误才修受影响依赖。
C02 核查M02实际模型尺度、设计、DMRcate调用/对象和167/660区域来源，
不根据“EWAS无显著CpG”直接认定DMR错误，也不换阈值获得更多区域。
C03 有效德国区域固定坐标到英国逐患者复测；两队列结果已经已知，
必须标为事后固定区域评估，不称新盲法验证，不互换队列角色。
C04 将M02探针QC、M值、细胞组成、分臂/启动子及共同支持任务逐项列状态；
没有真实年龄/批次保持原主要模型受阻，不能用简化模型冒充。
C05 复现M03供者级ROI和9项主检验，补完整供者分布、两臂、成员贡献、
真实切片/ROI图；扫描全部80个tar成员寻找尺度与图像，
没有可验证的尺度/边界则梯度NE，不拖住现有ROI结果。
C06 汇总632类真实模块删除的效应变化、方向保持和NE，
不把“可评价”自动叫“稳健”，不重建WGCNA或增加删除择优检验。
C07 准确解释M04/M05 DE及临床联接状态；AH有显著、MG无显著不等于交互。
C08 FinnGen、ALC/EDC保留真实授权和患者键边界；不自动发申请。
C09 完成后将有依据的新结果整合为作者审阅稿，
正文与全部正式编号补图/补表集中在对应Word/PDF，严格三线表；
不制作作者确认表，不将真实组织图替换为生成图。
C10 日志writer停止后封包；manifest不自引用，打包日志放目录外。

本包Python工具已做合成测试；R脚本尚未在编写环境运行。
请在已有DPN R环境先解析和跑tests/smoke_fixed_region.R，
再按SCHEMAS建立真实输入并执行；不要冒称它们已在患者数据验证。
如果本地已有有效的更具体锁定方法，先核对并保持，
不能默默用本包的新辅助实现替换主分析。

未取得资源就记精确状态，同时继续其他可行任务；
不新增队列、通路、TF或模型筛选，不改变阈值方向以保阳性。
不公开GitHub/Zenodo，不更新DOI、不投稿、不代填协议。

最终交付完整审阅ZIP与SHA256、实际代码/配置/结果、
M04计数对照、M02方法与固定区域复测、M03完整空间证据、
块删除汇总、未完成表及新稿件，不只返回十页摘要。
