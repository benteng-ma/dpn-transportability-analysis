# DPN v4 定向收尾执行任务

版本：2026-09-12 closeout 1。性质：在已知 v3.1、v4 和 targeted repair 结果后的方法核查、原定任务补齐与图文整合。

## 0. 直接执行的授权与范围

你现在位于作者的 DPN 项目中。请实际读取输入、编写/适配脚本、运行和交付，不要仅重述计划。本轮不新增疾病队列、模型筛选、全库通路或调控筛选；不再次执行已经通过输入和语义验证的全部历史分析。

允许：项目内读取；新目录内写入与本地计算；合法公开元数据核查；必要的项目局部依赖安装；从已经下载的归档中读取公开处理产品；重绘真实结果；生成审阅主稿和合并补充材料。

不允许：改变十个程序的成员、方向或资格来获得阳性；覆盖历史；按显著性删样本/选模型；伪造临床键/年龄/批次/尺度；代替作者发送邮件、填写访问表、接受新协议、付费计算、访问未授权受控资料、投稿或发布 GitHub/Zenodo/DOI。缺少访问授权只阻断相关模块。

审计阶段发现可重复的实施错误，可以在先记录错误及修复规则后开展**受影响依赖**的修正；不是不论发现什么都强制保持数值。每项变更需注明报告性更正、数值纠错、原定未完成任务或新增方法敏感性，不能默默混为一类。

本文件是完整任务。reference_only 中的旧文件仅供追溯，不是本轮锁定，也不是继续全部 v4 扩展的启动指令。不要把全任务塞进 AGENTS.md。逐段读到本文最后的结束标识，记录已读文件字节和 SHA256。

## 1. 先确认项目与当前有效版本

1. 项目根目录名称必须是 `phase0_6_human_dpn_stage_projection`。从当前工作目录或作者给出的实际路径定位；不要在其他六篇文章的目录中搜索或运行。
2. 找到实际 `targeted_repair_2026-09-12` 续作目录，其 STATUS、锁定方案、AMENDMENTS、完整 M02/M03/M04/M05 结果、原始脚本和日志。核对报告对应版本，而不是仅取修改时间最新的文件。
3. 回溯 v3.1 `strict_background_addendum/consolidated_results` 及最终替换清单。修复成员已报告 SHA256 为 `4a1a4a604366bab914ec04b98a06ec78863417e943b23e39fea913288308ce9c`；此值是核查提示，不允许为了匹配而改文件。
4. 原成员 P1..P10 依次为：early all cell 303/337；late all cell 1395/1707；late neuronal 141/302；severity parent 321/134；spatial parent 17/43；late core 48/80；neuron residual 86/199；all cell residual 1310/1596；severity core 16/7；severity residual 219/118。核查实际基因成员，不仅核对数量。
5. 新建 `analysis_v4/closeout_<actual_UTC_timestamp>`。本轮所有输出在其中。不要把建议的文件名伪装为历史已存在文件。
6. 写出逐任务 INPUT_BINDINGS.tsv：逻辑输入、真实项目相对路径、SHA256、记录键、实际字段、输入角色、来自哪个锁定版本。缺失字段不得静默选第一列/最后一列；查原脚本和表头建立显式适配器。
7. 所有原文件只读。登记新分支输入前后 SHA256；历史打包日志已发生的差异原样登记，不修改成全绿。
8. 先运行本包合成测试，再用真实数据独立核查。合成测试通过只说明相应函数在这些边界情形工作，不证明 DPN 数据或方法正确。

## 2. 本轮工作顺序及完成状态

C00 版本和输入绑定 → C01 M04 计数口径 → C02 M02 方法审计 → C03 固定区域跨队列复测。
C04 M02 未报告子任务清理、C05 M03 空间收尾、C06 固定模块删除汇总可在不共享写入的前提下并行。
C07 收紧 DE/临床解释，C08 外部权限状态，最后 C09 全文和结果包整合、C10 封包。

每个子任务单独记录：PLANNED、RUNNING、COMPLETED、COMPLETED_EXPLORATORY、DESCRIPTIVE_ONLY、NOT_EVALUABLE、NO_SELECTED_FEATURES、BLOCKED_RESOURCE、BLOCKED_ACCESS、BLOCKED_LINKAGE、FAILED、NOT_VERIFIED。

“父模块审计完成”不能代替“子分析完成”。结果为零且有效执行是完成；有脚本未运行不是完成；ROI 汇总完成不等于距离梯度完成；有若干区域不等于临床验证完成。

## C01. M04 背景计数与映射完整性

### C01a. 先解释报告数字，不预判一定又有统计错误
报告第5页写 58,174 个原始特征、35,749 个规范唯一 GeneID、15,430 个“全零 GeneID”、27,506 个最终 GeneID。35,749−15,430≠27,506。

逐行读原始表达、映射和过滤代码，建立两个不同层级的表：
- RAW_FEATURE_DISPOSITION.tsv：原始 row_id、原始 feature、候选 GeneID、唯一/歧义/未映射、有限值、原始全零、是否进入聚合、唯一排除原因。
- CANONICAL_GENE_DISPOSITION.tsv：每个唯一 GeneID 对应多少原始行、多少行进入聚合、聚合后有限/全零、最终是否进入排名、排除原因。

两表都包括排除项。把“原始行全零”“全部映射行中某行全零”“聚合后该 GeneID 全零”分开。分别对同一单位验证纳入+互斥排除=起始数量；不能拿不同层级数字硬做减法。

若仅表头/计数阶段不同：只修表述和计数，不重算疾病效应。若矩阵确实违反既定严格背景：先记录，限定重算 M04 的完整评分、两臂、依赖影响、图和旧有校正家族。表达背景与 DE 背景不同不自动是错误，分别说明目标与资格。

### C01b. 验证注释，不由“多出1198条”推出完整
确认缓存的真实原始来源、版本和过滤历史；将当前 feature 对同一固定官方注释的映射逐项对照。统计缓存覆盖、官方可解析但缓存缺失、冲突、歧义。含有不在血液数据的映射，只能证明缓存不完全等于该数据的行集合；不能单独证明其对当前研究完整。

禁止最小 P、最大效应、第一条/最后一条记录选基因。新版本注释不能替代旧快照后假装没变。正常记录无映射的 feature，不强行补齐。

交付：COUNT_FLOW.tsv、两张逐行去向表、ANNOTATION_COMPLETENESS.tsv、原报告计数到真实单位的对照。若数值改变，再交完整 OLD_NEW_M04.tsv 及依赖清单。

## C02. M02 甲基化方法审计：先确认 167/660 是什么

报告记载：315份 beta/detection-P 输入，231名 DPN；PROPGER 139、PROPENG 92；两个 EWAS 无 q<0.10 CpG，却有 167/660 DMR。

这不能仅凭表面现象判定为错误，也不能因此默认 DMR 正确。需要实际代码、函数版本、参数和注释对象。

1. 保存 R version、.libPaths、limma/minfi/missMethyl/DMRcate 版本，实际执行的 cpg.annotate、dmrcate、extractRanges、gsaregion 调用。保存安装版本 formals/body，不用联网文档默认值代替实际执行值。
2. 明确原输入 beta 或 M；`what='Beta'` 是否在函数内部转 M；完整 EWAS 与 DMR 注释是否用了同一队列、设计、系数、探针背景、比较方向、缺失规则。不能从“beta branch”标题就判断用了错误尺度。
3. 保存真实 design matrix（231不合并拟合两个队列）、painful minus painless 系数、样本顺序和探针键。核对 Detection P 列没有误当 beta。
4. 核对常染色体、SNP、交叉反应探针、multi-mapping 与多基因功能注释的区别。没有 IDAT 时不声称完成原始强度/noob QC；已做检测P QC仍保留。
5. 核对 `cpg.annotate` 的 fdr、what、arraytype、coef；注释对象中的 is.sig、ind.fdr、rawpval 与完整 EWAS是否一致。
6. 核对 `dmrcate` 的 pcutoff、lambda、C、min.cpgs，及提取后是否又按 Stouffer、Fisher、min_smoothed_fdr 或其他列过滤。实际字段名以当前版本为准，列出全部统计列及其含义。
7. DMRcate 的 pcutoff='fdr' 与手工数值 pcutoff 不是同一种阈值解释。不要在没有返回区域时换数值 pcutoff 直到有结果。若历史已用手工阈值，查实际锁定时间；缺少事前记录就标记未验证的探索选择，不能追述成原计划。
8. 复现167/660所在**原实现**，不把这两个数字当新实现必须达到的目标。只有查实输入、实现或声明不一致才做数值纠错。保留历史输出、原方法、修复方法、影响对照，不选择较多DMR的路径。
9. gsaregion 的 all.cpg 必须是该队列实际测试背景；hg19/EPIC版本与固定十程序 GeneID要匹配；区域不是“显著CpG种子”。P2/P8是相关的父/残余集合，不是独立两次复制。
10. 写 M02_METHOD_AUDIT.md 和实际参数JSON。将区域结果明确分类为可复現且声明一致、可复現但仅探索、定义未清、或应废弃的实施错误。不可用的 DMR 列表不进入下游“验证”。

提供的 `audit_methylation_runtime.R` 只取安装函数与存档对象信息，不替代上述真实调用审计。

## C03. 完成原定“德国固定区域→英国同坐标复测”

这是补齐已授权任务，但现在两队列结果已知；必须叫“既有发现区域的事后固定坐标评估”。不能叫新盲法验证、独立预注册确认，也不能互换发现和验证方向。

A. 先过C02。使用有效的 PROPGER 完整发现列表，不依据英国效应、P/q或是否重叠筛区域。有效列表为空时返回 NO_SELECTED_FEATURES；没有义务让此分支产生显著性。
B. 固定 region_id、hg19 chr/start/end、原始选取依据与方向。明确1-based closed与BED0-based转换；不得用英国各自调用的相近DMR替代固定坐标。
C. 通过核实的EPIC注释得到德国区域的固定探针集合；在英国使用同一区域内、按不参考英国关联结果的QC规则保留的探针。报告每区参考探针数、可测数、共同探针及覆盖。不能仅留下同方向CpG。
D. 优先执行原锁定中已有的固定区域检验。如果原方案只要求复测而没规定统计量，采用下面这套明确的收尾补充实现，并单独记录：
   - 每个样本取该固定区域合格CpG的平均 M 值；至少3个CpG沿用原区域最低数。
   - 有效beta以epsilon=1e-6处理精确边界以数值变换，缺失不补0或均值。beta来源质量规则先核实。
   - 每个区域以患者为n，采用与已归档探索分支一致的性别调整设计；固定 limma 实现与 robust/trend 参数。未规定参数时记录一个适用的唯一值再运行，不试多个挑小P。
   - 主要检验是平均区域M的双侧差异，不是DMRcate平滑统计量；两者不能称相同零假设。
   - 输出平均M效应、SE、未校正CI、原始P；beta差作独立标明的显示量。不能把beta差、M差和RNAlog2FC混用。
   - 全部德国被带入的区域构成一个复测家族。有效P按全带入名单长度进行BH，NE行P仍NA，明确family_n和finite_n，不只对“复现成功”者校正。这个缺失处理是本轮完整名单的实现政策，不修改旧的各队列EWAS/DMR q。
   - 比较方向用一致定义的beta效应。若德国区域内部方向混合或原方向无法核定，明确说明，不伪造正负复制。
   - 区域平均可发生相消。无均值支持不证明区域不存在任一CpG变化。
E. 未取得真实年龄/批次时，保留原主要模型BLOCKED；性别调整复测继续属于探索。数据若后来取得，另开主要模型分支，不覆盖探索结果。
F. PROPENG独有的P2/P8富集不能被本次复测追认成原发现。全部通过与未通过、NE、方向不一致都输出。
G. 若双队列集合共同支持检验尚未做，用两队列相同层级/相同对象的**原始集合P**计算max-P交并检验，对完整十程序名单校正，并标明“两队列都富集”，非RNA方向复制；不把q再当P输入。

交付：FIXED_DISCOVERY_REGIONS.tsv、REGION_PROBE_CROSSWALK.tsv、FIXED_REGION_RETEST_ALL.tsv、每样本区域M/beta、设计矩阵、所有排除、共同集合检验状态。提供的R脚本可作为D的实现，但要先建立并审计真实JOB.rds输入，不能根据示例自行编造数据。

## C04. M02剩余子任务逐项结案，不以“EWAS跑了”概括全部

列出实际结果路径、输入身份、完成状态或不可执行证据：
- IDAT获取/预处理/原始强度QC是否执行；已有beta分支不因此无效，但不能写成IDAT重算。
- beta→M实际路径；边界处理；检测P、SNP、交叉反应、性染色体探针清单。
- 真实年龄与技术批次；没有就BLOCKED，不以甲基化时钟、组均龄、GSM顺序或barcode推测批次替代。
- 原先规定的白细胞组成敏感性：只用可信测量或适配EPIC的固定外部参考及合格输入；说明估计比例的局限。无合格参考则NE，不把其引入当解决所有混杂。
- 无符号十集合、up/down次级集合、启动子限定敏感性、两队列共同支持；分别保留完整家族。
- 无q<0.05显著CpG时，gsameth选择特征分支是NO_SELECTED_FEATURES，不降低门槛、不取top100、不另找一套工具“补阳性”。不在本轮新增无阈值全集合检验替代原计划。
- 连续疼痛、MFD等没有真实患者键就NE。合法公开资料检索限于原GEO、原论文补表和作者提供文件；已知缺失不重复无限检索。

## C05. M03空间结果复核与完整展示

### C05a. 先复现已完成的区域结果
- 已下载9,698,846,720字节tar，48个选取处理文件已提取。先验证本地，不重复下载同一大文件。
- 逐表验证6 donor、7 DRG、16 section；条码使用section+barcode复合键。相同barcode可在不同切片重复，不能跨切片去重。
- 核对作者原ROI精确标签及当前修正版匹配字典。混合标签 `Nageotte also touching neuron`按锁定规则排除；single/multiple neuron及neurons的实际完整标签穷举登记。未知标签先报错，不能字符串包含关系偷偷归入神经元组。
- 使用同一合格背景和程序成员；真实零表达不是缺失。先在每section配对两种ROI求差，再DRG平均、再donor平均，最后各donor等权。不能先分别平均不配对的区域再相减。
- 排名/聚合操作必须与已经锁定的实现一致。若存档是spot分数平均而不是ROI伪bulk后评分，先说明两者不同，重现已有定义，不静默用另一定义替换。
- 全十程序保留，P9仍NE。条件资格是每臂10、总覆盖20%、每section每区域5个spot、至少3个合格供者，除非有更晚可核验锁定规则，不能为了维持6人或7阳性降低要求。
- 独立重算所有供者差、全部程序精确双侧符号翻转、现有完整BH家族；核对实际统计量是mean还是studentized，不能根据更小P选择。
- 6人时64个符号排列、包括观察排列，不用Monte Carlo plus-one重复加1。相同最低P=2/64及7项在9项家族内q约0.04018是核对提示，不是程序应硬写的结果。
- 符号翻转依赖相应对称/可交换性假设，ROI不是随机处理；把这一假设写入方法。bootstrap在供者层重抽并保持两臂协方差，CI不能用来覆盖多重校正结论。
- LODO只汇总效应方向和变化；5人精确P分辨率限制导致不能过0.05，不应把“删除一人后不显著”直接定义为不稳健。

### C05b. 补两臂和基因贡献
每一供者、每一程序记录ΔU、ΔD、ΔS=ΔU−ΔD；同一批合格配对观测、同一排名背景。基因贡献在该臂测得成员数下计算，并按同一层级汇总。对于可测成员在切片间不同时，先在各切片用自身真实分母计算再等权，不能最后用全并集分母重算。
所有两臂检验是独立的次级18项家族（按实际合格数记录），不替换9项完整分数主家族。已执行则只复现，未执行则记录为原定任务的后续实现。
逐基因贡献是算术解释而非gene-level验证；不根据贡献择选新程序；接近零的总差不报告夸张贡献百分比。

### C05c. 扫描完整80个归档成员，再决定距离梯度
- 不仅查已提取48项；扫描整个本地tar目录，寻找同切片scalefactors_json.json、真实组织图、CLOUPE、像素与ROI坐标说明。先列目录再提取需要的小文件，不重复HTTP请求。
- 用官方标注和匹配检查关联影像、坐标和尺度。不从其他切片借scalefactor，不把阵列坐标、像素和微米混用。
- 10x 的 spot_diameter_fullres主要是估计/显示尺度，不是独立显微镜物理标定。若仅有该字段，可在原计划允许且实际匹配时按“估计spot直径单位”解释，不能声称精确微米。
- 原计划0–2d、2–4d、>4d距离层仅在ROI边界、坐标和所需尺度确实可验证时执行；不以最近结节spot中心默默替代ROI边界，除非有原锁定定义。
- 每section汇总斜率/秩关系并到donor层；空间重抽保持组织掩膜及邻域结构。不能把数千spot的普通独立P称供者P。
- 找不到真实尺度/边界则该子任务NE；不影响已完成ROI对比。不要新发明像素阈值或换分箱恢复显著。

### C05d. 作一组真正的空间图
主图候选包括：真实H&E及作者ROI覆盖；16/7/6层级和合格spot分布；全部程序效应与区间；6名供者配对变化；ΔU与−ΔD贡献；有条件的已校准梯度。
挑代表切片按预先明确的QC/完整性和供者ID顺序，不按效应挑最强；全部16切片在补充提供。没有原始组织图，就绘真实坐标散点并标为坐标图，不能生成或复原“看起来像”的H&E。
不要求有多少个显著panel；图必须可读，每个panel有唯一数值/图像来源。结果不能叫独立DPN空间验证、细胞内机制或甲基化到RNA的因果链。

## C06. 632项固定模块删除的解释性总结

1. 汇集M01、M04、M05的当前有效BLOCK_INFLUENCE及任务登记。核对gene/module的实际两列、九个非灰模块、4,368个规范GeneID；632是报告记录数提示，不强制输出恰好632。
2. 对全部源×终点×程序列出：原效应、每个固定块删除后的效应、删除了多少up/down成员、剩余成员、是否仍可评价、排名背景是否不变。
3. 汇总最大与中位绝对效应变化、同方向删除比例、最有影响的全部并列模块、NE数；基线接近零时只报告绝对变化，不做失真的“贡献百分比”。容差仅作数值处理，在锁定文件中列明。
4. 零成员重叠、不满足门槛、grey/unassigned分别记状态，不把一次删除全部程序成员算有效敏感性。不存在的块不能以选择其他聚类代替。
5. 不重跑WGCNA、不改模块、不用删除后更小P挑模型；默认不为632次删除增加独立显著性检验。影响分布不是临床复制。
6. 画完整效应保留/变化图，并结合已存在的模块细胞注释作描述；不把模块标签或连接性称因果靶点。

## C07. M04/M05全基因和相关临床任务收尾

保留现有全基因分析；当前报告M04主要3959、性别敏感性2456，M05 AH1288、MG0、交互0均是核查提示，不是目标。

- 核实 M04 主/敏感性实际测试基因数为何不同（16,702与17,321）：若filterByExpr依design产生差异，说明设计和检验背景；不能仅比较两个显著基因数便声称“性别解释了多少”。保留两完整列表并在共同基因中作描述对照，不重挑主模型。
- M04的疾病、神经部位、手术来源完全混杂不能靠sex调整消除；标题图注用“Morton来源比较”，不称非糖尿病机制或DPN特异性验证。
- M05 AH有DE、MG没有DE，不等于疾病×肌肉显著。原交互无支持如实保留，不宣布已证明“仅AH受影响”。AH/MG按原数据字典展开名称；不从缩写猜。
- 对 M04阵发/诱发疼痛、M05病理/功能关联列出确切联接状态。有真实官方键按原任务执行有限完整家族；没有则NE，不用组标签/原文章组均值代替个体量。
- 不从几千个DEG另选hub再开PPI、富集或预测筛选。已授权全基因结果仅提供背景。

## C08. 不会因缺权限而无限等待的任务

M07 FinnGen/GTEx：未获正式可用访问资料就BLOCKED_ACCESS。不能用浏览器lead SNP代替区域密集数据。若作者已自行完成访问，按原锁定DF13临床终点和既有方法执行；不自行换更新版本或更显著终点。原同意本轮代码执行不等于授权接受协议。

M08 ALC/EDC：继续不按顺序联接，不自动发申请。只核对正式键是否已到；未到则结案BLOCKED_LINKAGE/ACCESS。保留已有草稿，不生成一组重复邮件占充工作量。

丢失的历史REVIEW_CN/来源说明字节是来源管理问题，不应阻断已有真实输入的M02/M03科学任务。可以从reference_only恢复真实旧字节并写当次收件时间；不能倒填上一轮已收到。

## C09. 整合稿件，不再把有价值结果埋成状态报告

完成上面收尾后，生成一个新的**作者审阅版**主稿、投稿信草稿和合并补充材料。不自动投稿或宣称已可直接投稿。

主文建议顺序：研究设计和固定对象；人神经病例关联与两臂解释；新增病理区域证据；外部病理/其他组织适用范围；有边界的甲基化/治疗结果；方法评价。显著和不显著都保留，但不以运行日志作为结果主线。

- 源程序发现、DPN病例比较、糖尿病史供者空间ROI、疼痛DPN甲基化、Morton来源比较与肌肉DE是不同证据，不拼成已验证的因果链。
- 空间证据可能加强组织解释，不恢复severity-core旧轴突阳性；分解优越性仍未建立；病例关联不能写临床预测/疗效。
- 报告P/q实际家族；不得挑较小的单双侧口径；不能把方向为负叫没发现，也不能把负向称同向复制。
- 主文已有7图+1表是旧工作版结构，不为凑数字新增重复图。给M03合理实证空间，保留旧有效结果于适当正文/补充位置；按现行目标期刊限制重排并记录图表去向，不删全部阴性来突出空间。
- 不压缩成只有一两张总览森林图。供者分布、区域图、效应、两臂与限制分别可见；panel数量不是发现数量。
- 标题不含连字符；英语自然、客观。全体作者同意/独家投稿沿用作者此前实际指示，不添加确认表或虚構新伦理批准、基金、ORCID。
- 补充方法先把实际统计量、家族、软件、DMR定义、ROI层级说明完整，再放所有编号补图和补表；一个Word和/或PDF集中可阅读材料。
- 严格三线表：顶线1pt、表头底线0.5pt、底线1pt；禁用内部横竖线和底色。跨页需要清晰续表与重复表头，不能仅设XML就声明逐页检查。
- 逐CpG/逐基因等巨大源表单独TSV.gz可用，但逐行完整与摘要表必须分清，不能用Excel作为唯一补充材料。所有正式编号补图/补表都在合并读者文档。
- 真实切片不得用生成式图片替代；第三方图源许可和出处保留；提取现有图不等于重新测量。

交付SOURCE_REGISTER、INDEPENDENCE_REGISTER、MULTIPLICITY_FAMILIES、CLAIM_EVIDENCE_REGISTER、PANEL_SOURCE_REGISTER，包含确切结果路径、记录键和源代码hash。全文每一个新主张都有实际数据来源，不因为检查数多而升期刊。

## C10. 验证与无自相矛盾封包

最低必须独立核对：
1. M04同单位逐级计数相加、缓存映射、实际完整背景排名；与程序限定背景反例确实不同。
2. 两臂代数、所有供者层差异；全部9个空间主P/BH，P9保持NA/NE；64枚举没有重复加1。
3. M02模型尺度、设计、真实coef、全部探针家族，DMR参数对象与实际输出对应。
4. 固定德国区域到英国的坐标/探针联接、无英国P参与边界选择、整家族P/BH与NE。
5. 632类任务的可评价与实际块数、背景恒定、删除后覆盖、完整影响汇总。
6. 不把“AH显著MG不显著”写成交互；不把beta/M、RNA倍数、rank差混用。
7. 原始文件前后hash。原历史161/162日志差異照实报告，不偷偷改历史manifest。
8. 关闭全部科学进程和日志writer后才封包。包装日志写在待封目录外；生成MANIFEST后不再写内部日志，不把ZIP或自身hash放回被hash的目录。
9. 打包后复核每个ZIP成员与原文件hash、目录完整性和ZIP CRC；ZIP的SHA256单独sidecar。没有git commit就给代码hash，不虚构commit。
10. 图文渲染并检查实际页面，报告检查范围。自动文本检查不是人工逐页视觉审阅；没有查看不能写全部看过。

提供代码可用于对应核查。未知本地列名由Codex依据真实文件建立adapter，不让作者手工猜字段；adapter本身保存源路径、输入hash和1:1/1:many关系检验。

## 3. 最终必须交付的文件与回答

在新输出目录按以下结构保存实际文件（未完成只写状态，不制造空“完整结果”）：
```
00_admin/   实际输入绑定、锁定、修订、依赖、状态、环境
01_M04_flow/  两级去向、COUNT_FLOW、注释覆盖、必要时旧新结果
02_M02_audit/  实际调用、函数版本、对象结构、QC、参数和审计判断
03_M02_fixed_regions/  全部固定区域、探针键、设计、逐患者分数及复测结果
04_M03_spatial/  全层级/QC、全部供者/ROI、两臂/成员、原图/坐标、梯度状态
05_block_summary/  全632类任务、完整汇总、图源
06_remaining_status/  M02/M04/M05临床子任务和M06/M07/M08明确状态
07_figures/  可编辑矢量、300/600dpi按图种类的TIFF、每panel源表
08_manuscript/  新审阅主稿、投稿信草稿、合并补充Word/PDF
09_verification/  真正数值与语义检查、差异/失败日志
10_readout/  CLOSEOUT_REPORT_CN.md、TASKS_NOT_DONE.tsv、NEXT_ACTIONS.md
MANIFEST.tsv
```

完整审阅ZIP必须包含这次续作的**实际**脚本、锁定配置、足以重算关键结果的最小处理输入或明确的合法取得方式、全结果和登记。不要只发送十页总结而不提供本轮代码。个人绝对路径可留在授权内部运行日志，投稿公开版清理；不写入访问令牌和直接识别患者的信息。

最后答复作者四件事：
1. 哪些问题只是报告口径，哪些查实影响数值，具体改了什么。
2. M02固定区域复测、M03ROI/两臂/梯度、632项删除分别完成了什么；未完成是什么原因。
3. 这次哪些结果新增、哪些重复旧证据、哪些不能称独立临床支持。
4. 可核验ZIP和SHA256、可读报告、修订主稿路径；不得把本机路径写成用户已上传到聊天的附件。

若受运行时限中断，保存准确检查点、仍在运行的PID或已停止状态、恢复命令。不要声称离线后台还会继续。不要通过缩减测试家族、换阈值或删输入来结束任务。

**现在先执行C00，然后C01和C02；尽快完成C05的现有空间证据复核与展示。独立受阻任务不拖住其他模块。不要再给作者返回一份新的泛化规划。**

---
END_DPN_V4_CLOSEOUT_TASK
